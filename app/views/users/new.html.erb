<%= render 'shared/logo' %>

<%= render partial: 'shared/path', locals: {
    link_layers: [
        { name: "注册" },
    ]
} %>

<div class="center-block container row">
  <div class="col-md-4 col-md-offset-4">
    <%= render partial: 'form', locals: { form_method: :register } %>
  </div>
</div>